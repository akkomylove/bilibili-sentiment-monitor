# B站舆情监控平台 - 用户使用指南

## 一、启动方式

### 方式1：双击启动（推荐）
在项目文件夹中双击 `start.bat` 文件，会自动：
1. 启动 Docker 容器（MySQL + Redis）
2. 启动 FastAPI 后端服务
3. 自动打开浏览器访问 `http://localhost:8000`

### 方式2：手动启动
如果 `start.bat` 无法使用，可以手动按顺序执行：

```bash
# 1. 启动数据库（在 PowerShell 中）
docker compose up -d

# 2. 启动后端服务（在另一个 PowerShell 窗口中）
uvicorn app.main:app --host 0.0.0.0 --port 8000
```

然后浏览器访问 `http://localhost:8000`

---

## 二、页面功能说明

### 1. 仪表盘（/dashboard）
**用途**：查看全局数据汇总和分析图表

| 区域 | 说明 |
|------|------|
| 顶部统计卡片 | 视频总数、评论总量、监控关键词数、数据质量评分 |
| 情感趋势图 | 近30天正面/中性/负面情感变化曲线 |
| 情感占比饼图 | 整体情感分布比例 |
| 关键词词云 | 高频词汇可视化 |
| 话题聚类图 | 评论话题分类散点图 |
| 关键词管理 | 可添加/删除关键词（快捷入口） |
| 最近视频 | 显示最新采集的视频列表 |

**注意**：这里的"触发全量采集"会同时触发所有关键词的采集任务，频繁点击可能触发 B站反爬机制。

---

### 2. 监控配置（/monitor）⭐ 核心页面
**用途**：配置你想监控的 B站关键词，系统自动采集数据

#### 关键词列表
| 字段 | 说明 |
|------|------|
| 关键词 | 要监控的搜索词，如"Python教程" |
| 分区 | 可选，限制搜索范围（如"科技"） |
| 间隔 | 自动采集间隔（秒），建议 >= 300（5分钟） |
| 状态 | 启用（自动采集）/ 停用（暂停采集） |
| 上次采集 | 最近一次成功采集的时间 |
| 视频/评论 | 该关键词已采集到的数据量 |

#### 操作按钮
| 按钮 | 功能 | 反爬风险 |
|------|------|----------|
| ⬇️ 立即采集 | 手动触发该关键词的采集 | ⚠️ 中风险，不要连续快速点击 |
| ⏸️ 停用/▶️ 启用 | 切换自动采集状态 | 无风险 |
| 🗑️ 删除 | 删除该关键词配置 | 无风险 |

#### 快捷操作区
| 按钮 | 功能 | 反爬风险 |
|------|------|----------|
| 触发全量采集 | 同时触发所有"启用"状态关键词的采集 | ⚠️ 高风险，谨慎使用 |
| 触发全量分析 | 对已有数据执行情感/关键词等分析 | 无风险 |

#### 最近采集动态
显示历史采集记录，帮助了解系统运行状态。

---

### 3. 视频列表（/videos）
**用途**：查看已采集的所有视频

- 支持按标题关键词和分区筛选
- 点击视频标题进入详情页
- 可手动触发单视频的深度评论采集或弹幕采集

---

### 4. 数据治理（/governance）
**用途**：查看数据质量报告和治理规则

- 数据质量评分
- 治理规则配置（去重、清洗、脱敏等）

---

### 5. 报告导出（/report）
**用途**：导出分析结果为 CSV/JSON/Markdown 格式

---

## 三、反爬机制与使用建议

### B站反爬规则（已知）
1. **IP 限制**：短时间内大量请求会暂时封禁 IP（通常 10-30 分钟）
2. **频率限制**：搜索 API 建议间隔 >= 5 秒，评论 API 建议间隔 >= 1 秒
3. **Cookie 验证**：部分接口需要登录 Cookie

### 安全使用建议
| 场景 | 建议操作 |
|------|----------|
| 日常监控 | 使用"监控配置"页面添加关键词，依赖自动定时采集（每5分钟） |
| 新增关键词 | 添加后等待自动采集，或手动触发一次，然后等待至少 5 分钟再触发 |
| 急需数据 | 可以手动触发，但触发后等待 5-10 分钟再触发下一次 |
| 大规模采集 | 不要同时触发多个关键词，逐个触发并间隔 5 分钟以上 |

### 风险等级说明
- 🟢 **无风险**：查看页面、切换关键词状态、查看分析结果
- 🟡 **低风险**：添加/删除关键词配置
- 🟠 **中风险**：手动触发单个关键词采集（偶尔使用）
- 🔴 **高风险**：频繁点击"立即采集"或"触发全量采集"

---

## 四、常见问题

### Q1: 页面显示"暂无数据"
**原因**：刚添加关键词，还没有执行采集。
**解决**：等待 5 分钟（自动采集），或手动触发一次采集。

### Q2: 采集任务提交后没有新数据
**原因**：B站 API 返回空结果或被封禁。
**解决**：等待 10-30 分钟后重试，检查网络连接。

### Q3: 如何停止所有自动采集？
**操作**：在"监控配置"页面，将所有关键词的状态切换为"停用"。

### Q4: 数据存在哪里？
**回答**：MySQL 数据库（Docker 容器），数据持久化在 Docker Volume 中。

---

## 五、系统架构速览

```
用户浏览器
    ↓
FastAPI 后端 (localhost:8000)
    ↓
MySQL 数据库 (localhost:3307)  ← 存储视频、评论、关键词等数据
Redis (localhost:6380)          ← 缓存和任务队列
Celery Worker                   ← 后台执行采集和分析任务
    ↓
B站 API                         ← 获取视频、评论、弹幕数据
```

---

## 六、技术参数

| 组件 | 地址/端口 |
|------|----------|
| Web 页面 | http://localhost:8000 |
| API 文档 | http://localhost:8000/docs |
| MySQL | localhost:3307 |
| Redis | localhost:6380 |

| 自动任务 | 频率 |
|----------|------|
| 自动采集 | 每 5 分钟 |
| 自动分析 | 每 30 分钟 |
